{# ssh -o StrictHostKeyChecking=no "$user@$host" #}
Host {{ node_ip.split('.')[0] }}.{{ node_ip.split('.')[1] }}.{{ node_ip.split('.')[2] }}.*
    User {{ abm_install_user }}
    Port 22
    StrictHostKeyChecking no
    IdentityFile ~/.ssh/{{ ssh_key_name }}
